services:
  - type: web
    name: calculadora-oc-web
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: cd calculadora/web && gunicorn app:app
    healthCheckPath: /
    autoDeploy: true
